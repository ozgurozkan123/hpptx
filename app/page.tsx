export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
      <h1>hpptx MCP server</h1>
      <p>This project exposes the MCP endpoint at <code>/mcp</code>.</p>
    </main>
  );
}
